patch:
  recognizer/patterns/reverse_lookup:
  switches:
    # reset: 0  → 默认取 states 第 0 项
    # reset: 1  → 默认取 states 第 1 项
    # 不写 reset → 记住上次设置的状态，不会自动重置
    - name: simplification
      states: ["漢字", "汉字"]
      reset: 1
