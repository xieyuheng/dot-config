#!/bin/sh
# 在此处使用双引号包裹 "$(xcwd)"，以确保当 xcwd 输出包含空格的路径时
# 整个路径字符串作为一个整体传给 urxvt -cd，不会被 shell 拆分成多个单词。
# 不在 ~/.xbindkeysrc 中直接处理引号转义，是因为 xbindkeys 的命令解析机制
# 对嵌套引号的支持不可靠，独立脚本能彻底避开这个问题。
exec urxvt -cd "$(xcwd)"
